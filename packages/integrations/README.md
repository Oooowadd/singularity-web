# @goooose/integrations

外部集成层(框架无关,无 `@goooose/*` 依赖)。

`src/clients/`(TikHub / XHS / ASR / yt-dlp / YouTube Data / LLM / vision / references)· `src/proxy/`(代理池 + 带重试抓取)· `src/utils.ts`(parseLlmJson / safeText / sleep 等共享 helper)。
