# @singularity/prompts

LLM 提示词模板(核心 IP)。纯字符串构造函数,无依赖。

`clerk`(视频分析 / SOP)· `clerk-series` · `clerk-comments` · `poet`(写稿 / 大纲 / 扩写 / 圣经 / 选题 / humanizer)· `muse`(爆款触发 / 选题生成)。

渲染版目录见 `notes/prompts_catalog.md`(脚本生成,勿手改;重跑 `packages/db/scripts/gen-prompts-catalog.ts` 刷新)。
