# @singularity/shared

Shared Zod schemas, TypeScript types, and **prompt templates** — the core IP.

## Layout (planned)

- `prompts/` — port 1:1 from archive `backend/app/prompts/*.py` (clerk / muse / poet)
- `schemas/` — Zod schemas mirroring Drizzle tables for tRPC contracts
- `types/` — shared TypeScript types

## Status

Week 0 placeholder. Prompts ported Week 3+ (Clerk → Week 3, Muse → Week 4, Poet → Week 5-6).
