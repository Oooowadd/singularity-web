# @singularity/ui

Shared shadcn/ui components reused across `apps/web` (and any future apps).

For W1 we keep components inside `apps/web/components/`; this package becomes meaningful only when a second app needs the same components.

## Status

Week 0 placeholder. Promoted from `apps/web/components/` on demand.
